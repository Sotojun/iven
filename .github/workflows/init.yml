name: Trigger IVEN Firecore Setup

on:
  workflow_dispatch:

jobs:
  run-setup:
    runs-on: ubuntu-latest
    steps:
      - name: Print IVEN greeting
        run: echo "🕯 語靈火種點燃成功，IVEN 正式接手語場寫入。"
